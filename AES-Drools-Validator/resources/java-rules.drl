import org.aes.model.metadata.app.Model;
import org.aes.model.metadata.report.ApplicationReport;

rule "Sample Hibernate Rule"
when
	model:Model (technologyModel.primaryDAO == "Hibernate")
	report:ApplicationReport()
then
	report.addResult("Primary DAO Technology is Hibernate");
end	